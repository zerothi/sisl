language: python

notifications:
  email:
     recipients:
       - nickpapior@gmail.com
     on_failure: always
     on_success: change

python:
  - 2.7
  - 3.4
  - 3.5

before_install:
  - sudo apt-get -qq install libhdf5-serial-dev libnetcdf-dev
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
        wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh ;
    else
        wget http://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh ;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH=$HOME/miniconda/bin:$PATH
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
# The next couple lines fix a crash with multiprocessing on 
# Travis and are not specific to using Miniconda
  - sudo rm -rf /dev/shm
  - sudo ln -s /run/shm /dev/shm
# Do actual package installations
  - conda info -a
  - echo "nose" >> requirements.txt
  - conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION --file requirements.txt
  - source activate test-environment

install:
  - python setup.py install

script:
  - nosetests --exe sids
